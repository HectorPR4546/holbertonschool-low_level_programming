#include <stdio.h> /* Main used to compile this function */
/* Task # 4, using puts command and Betty style */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n);
	return (0);
}
